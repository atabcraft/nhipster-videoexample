version: '2'
services:
  videoexample-postgresql:
    image: postgres:12.1
    # volumes:
    #     - ~/volumes/jhipster/videoexample/postgresql/:/var/lib/postgresql/data/
    environment:
      - POSTGRES_USER=videoexample
      - POSTGRES_PASSWORD=
      - POSTGRES_HOST_AUTH_METHOD=trust
    ports:
      - 5432:5432
